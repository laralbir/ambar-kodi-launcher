Flask==3.0.0
requests==2.31.0
spotipy==2.23.0
pywebview==4.4.1
pyinstaller
Pillow
Flask-SocketIO==5.3.6
websocket-client==1.7.0
simple-websocket

# VU-meter: captura de audio del sistema (loopback), una dependencia nativa
# distinta por plataforma (ver ambar/adapters/audio/).
soundcard; sys_platform == "win32"
pyobjc-framework-ScreenCaptureKit; sys_platform == "darwin"
pyobjc-framework-CoreMedia; sys_platform == "darwin"

# Control de volumen del sistema (ver ambar/adapters/audio/*_volume.py).
# En macOS se usa `osascript` (ya viene con el sistema, sin dependencia extra).
pycaw==20251023; sys_platform == "win32"
comtypes; sys_platform == "win32"

# Ahora-suena/control de Spotify via SMTC en vez de su Web API, cuando el
# Spotify de escritorio esta sonando en esta misma maquina (ver
# ambar/adapters/media_session/windows_smtc.py) -- paquetes modulares
# pywinrt, NO el paquete "winsdk" (su sucesor oficial): a fecha de escribir
# esto no publica rueda para Python 3.13, y "winsdk" si intenta compilar
# desde fuente (requiere Visual Studio, no instalado aqui).
winrt-Windows.Media.Control==3.2.1; sys_platform == "win32"
winrt-Windows.Storage.Streams==3.2.1; sys_platform == "win32"
winrt-Windows.Foundation.Collections==3.2.1; sys_platform == "win32"
